const RideTicket = ({ width, height }: { width: string; height: string }) => (
  <img
    alt="Ride Ticket"
    src={"/images/RideTicket.png"}
    style={{ width: width, height: height }}
  />
);
export default RideTicket;
